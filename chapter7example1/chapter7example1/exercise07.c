/*#include <stdio.h>

int main() {
    
    int score = 0;
    int scoreSum = 0;
    double ave = 0.0;
    int i = 0;
 
        for(i = 1; i <= 10; i++){
 
            printf("점수를 입력하세요 : ");
            scanf("%d", &score);
 
            if(score < 0 || score > 100){
 
                printf("다시 입력하세요. \n");
                i--;
 
            }else{
 
                scoreSum = score + scoreSum;
 
                }
 
            }
 
        }
 
    ave = (double)scoreSum/(i-1);
    printf("평균은 %.1lf 입니다. \n", ave);
    
    return 0;
}

*/
